version = "0.1.0"
author = "Brian Dombroski"
description = "POSIX single-file multi-language script runner for Nim, Go, and Mojo"
license = "MIT"

bin = @["shebangsy"]

requires "nim >= 2.0.0"
requires "argsbarg >= 1.4.0"
